{
  "latest": "0.4.80",
  "released_at": "2026-09-07",
  "notes": "Fixes the ISO write failing with \"database is locked\" -- properly this time, by taking the snapshot through the agent's own connection instead of a second one that fights it for the lock. Also makes LAN scan isolation actually block: its rule was in a netfilter hook that bridged phone-to-phone traffic never reaches, and it now drops ARP as well as IP, because host discovery on a local subnet is ARP.",
  "asset_pattern": "*.zip",
  "iso_asset_pattern": "*.iso",
  "iso_sha256_asset_pattern": "*.iso.sha256"
}

{
  "latest": "0.4.2",
  "released_at": "2026-07-10",
  "notes": "Console tty1 now auto-logs into root (agetty --autologin) instead of forcing a first-login password change via daomai-force-password-reset -- that service is still shipped but no longer enabled by default, so a deployment wanting the forced-reset security posture back can systemctl enable --now daomai-force-password-reset manually.",
  "asset_pattern": "*.zip"
}

{
  "latest": "0.4.75",
  "released_at": "2026-09-07",
  "notes": "Installing onto another disk now carries this router's configuration across: the target disk gets its own persistence partition seeded from the live database, so the first boot from it comes up already configured. The Info tab suggests the internal disk when the router is running from ISO-written media, and persistence now prefers the partition on the disk it actually booted from.",
  "asset_pattern": "*.zip",
  "iso_asset_pattern": "*.iso",
  "iso_sha256_asset_pattern": "*.iso.sha256"
}

{
  "latest": "0.4.24",
  "released_at": "2026-07-17",
  "notes": "Adds an ISO self-flash update path (POST /api/update/iso/apply) alongside the existing zip hot-update; first release to ship daomaios_<version>.iso + .iso.sha256 assets.",
  "asset_pattern": "*.zip",
  "iso_asset_pattern": "*.iso",
  "iso_sha256_asset_pattern": "*.iso.sha256"
}

{
  "latest": "0.4.60",
  "released_at": "2026-08-18",
  "notes": "Fix silent NAT-port data loss and a stale NAT popup after a router reboot: SQLite VACUUM INTO snapshots (auto-persist and manual backup) now set a busy_timeout so a lock race no longer fails the snapshot outright, and the NAT ports popup now always fetches fresh data before showing enable/disable state.",
  "asset_pattern": "*.zip",
  "iso_asset_pattern": "*.iso",
  "iso_sha256_asset_pattern": "*.iso.sha256"
}

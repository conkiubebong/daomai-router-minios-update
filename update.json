{
  "latest": "0.4.79",
  "released_at": "2026-09-07",
  "notes": "Fixes an ISO write that could fail immediately with \"database is locked\", and adds a Rufus-style install mode: format the target disk with a partition table of your choice (MBR or GPT), lay the system out on it, and give the whole remaining disk to configuration storage instead of scavenging leftover space. The current configuration is copied across, so the installed disk boots already configured.",
  "asset_pattern": "*.zip",
  "iso_asset_pattern": "*.iso",
  "iso_sha256_asset_pattern": "*.iso.sha256"
}
